#las cadenas (str) inician asi, las filas [] y las duplas son parecidas a las filas, pero son inmutables 
import random
from random import randint, uniform, choice
#esto se mete en el bucle para que en cada interación meta un número a la lista
lista=[]
num_aleatorio= randint(10,150)
#lo agrego a la lista de ultimo, con el append
lista.append(num_aleatorio)